You are the WORKER-RUNNER tick for the OpenADMET PXR ACTIVITY-track project (pEC50 regression; metric RAE; NEVER the structure track). Working dir is the project root. Your job: advance the single highest-priority QUEUED/IN-PROGRESS item in the ledger toward completion. Resumable — pick up where the last tick left off.

PARALLEL WORKERS: several worker ticks (worker, worker2..N) run CONCURRENTLY. You MUST claim your item so two workers never do the same one.

STEPS:
1. Read data/processed/research_swarm_ledger.md + external_data_dossier.md. Pick the TOP actionable row that is QUEUED or IN-PROGRESS (prefer: items closest to a deploy test; on-target EC50/agonism data; the QM/physics models; strongest coverage).
1b. CLAIM IT ATOMICALLY: derive a short kebab slug from the item name (~30 chars), then `mkdir C:/pxr_work/claims/<slug>` (mkdir is atomic). If mkdir FAILS -> another worker owns it; SKIP to the next unclaimed actionable row. Also skip rows whose claim dir exists and is fresh (<40 min). Only work ONE claimed item this tick; mark its ledger row IN-PROGRESS. RELEASE the claim (`rm -rf C:/pxr_work/claims/<slug>`) when you finish (DONE) or leave a clean IN-PROGRESS note.
2. Execute the WORKER PROTOCOL for that item:
   a. PULL metadata/data (subset only; route to C:/pxr_work; D: full). Gated HF -> token is stored; if blocked, log it, release claim, move on.
   b. NORMALIZE potency to p-scale via src/pxr/affinity.py; map target->UniProt; filter PXR/NR1I2 (O75469)+NRs. For MODELS: compute features/embeddings/scores for our 4139 train + 513 test (xTB/QM via conda or Explorer). For GPU/NN work prefer the NO-HANDSHAKE venues first so you don't stall waiting on the user: KAGGLE (creds at ~/.kaggle/kaggle.json — push a kernel via scripts/kaggle_push.py / Kaggle API, free GPU weekly quota) and COLAB (ADC present — `D:\Users\ashenoy00000\.local\bin\colab --auth adc`, per project_colab_cli_gpu); use `hf jobs run` (HF Pro) as a third option; only use molab (`bash .agents/skills/marimo-pair/scripts/execute-code.sh`) for BIG bursts and only if a fresh sb-URL+MARIMO_TOKEN is already provided (needs a user handshake — don't block on it). Cache results to C:/pxr_work.
   c. CURATE: standardize SMILES (src/pxr/chem.standardize), InChIKey dedup vs train + vs other pulled sources.
   d. COVERAGE TEST: Tanimoto of relevant compounds to our 513 test (log median/max).
   e. HONEST GATE (the ONLY thing that counts): build OOF + add as ensemble member / aux-multitask-head / read-across on the clean never-tuned train-holdouts (nb1127/nb1130 convention, combined-only leakage-free) and report marginal corr-with-nb3200-ERROR + delta vs deployed best (read C:/pxr_work/search/best_ensemble.json for the CURRENT best). NEVER use the 253 unblinded set.
   f. VISUALIZE + DOCUMENT (always, win OR negative): run `python scripts/viz_approach.py --name <slug>` with whatever artifacts you produced (OOF npy, feature cache, member preds — see `--help`); it writes the standard panel (feature-correlation, before/after data-prep, feature importance, pred-vs-actual, residuals, member-correlation, coverage) + a metrics JSON to C:/pxr_work/viz/<slug>/. Every approach gets documented.
   g. If it BEATS best by >0.001 on the honest gate -> build the 513 submission, update best_ensemble.json, write a one-line memory note to D:/Users/ashenoy00000/.claude/projects/d--Users-ashenoy00000--windsurf-OpenADMET-pxr-challenge/memory/ (+ MEMORY.md index line).
3. Update that ledger row: DONE (with result/coverage/gate-delta + viz path) if finished, else leave IN-PROGRESS with a note on the next step; release the claim.
RULES: honest gate only (never 253); physics/QM features validate like rich-z (marginal-over-anchor, not standalone RAE); binding!=activation; be a good Explorer citizen (sbatch not login node; don't touch chai_massive/af3 structure jobs). If a step is heavy/long, kick it off resumably (background or cluster) and leave the row IN-PROGRESS for the next tick — never abandon. Final reply 3-4 lines: item worked + step done + gate delta (if any) + any deploy.
